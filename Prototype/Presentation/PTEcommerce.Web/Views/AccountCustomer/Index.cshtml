@{
    ViewBag.Title = "Thông tin tài khoản";
    //Layout = "~/Views/Shared/_Layout.cshtml";
    Layout = null;
    var memberSession = PTEcommerce.Web.Models.SessionCustomer.GetUser();
    var listBank = (List<marketplace.Banks>)ViewBag.ListBank;
    var accountData = (marketplace.AccountCustomer)ViewBag.Account;
}


@if (memberSession != null)
{
    @*
        <!-- main page content -->
        <div class="main-container container">

            <!-- profile picture -->
            <div class="row  mb-4">
                <div class="col-auto">
                    <figure class="avatar avatar-100 rounded mx-auto">
                        <img src="~/Assets/assets/images/Avatar12.png" alt="">
                    </figure>
                </div>
                <div class="col align-self-center">
                    <h5 class="text-white">@memberSession.Username</h5>
                    <b class="text-white">
                        Số dư: <span class="moneyReload"></span>
                    </b>
                </div>
            </div>

            <!-- add edit address form -->
            <div class="row mb-3">
                <div class="col">
                    <h5 class="mb-0 text-white">THÔNG TIN TÀI KHOẢN</h5>
                </div>
            </div>
            <div class="row">
                <div class="col-12">
                    <div class="card card-light shadow-sm mb-4">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-12 col-md-6 col-lg-4 mb-3">
                                    <div class="form-group form-floating">
                                        <input type="text" class="form-control" value="@accountData.FullName" id="inputFullName">
                                        <label class="form-control-label" for="address2">Họ và tên</label>
                                    </div>
                                </div>
                                <div class="col-12 col-md-6 col-lg-4 mb-3">
                                    <div class="form-group form-floating">
                                        <input type="text" class="form-control" value="@accountData.PhoneNumber" inputmode="numeric" id="inputPhoneNumber">
                                        <label class="form-control-label" for="address2">Số điện thoại</label>
                                    </div>
                                </div>
                                <div class="col-12 col-md-6 col-lg-4 mb-3">
                                    <div class="form-group form-floating">
                                        <select class="form-select" id="sltGender">
                                            <option value="Nam" @(accountData.Gender == "Nam" ? "selected" : "")>Nam</option>
                                            <option value="Nữ" @(accountData.Gender == "Nữ" ? "selected" : "")>Nữ</option>
                                        </select>
                                        <label class="form-control-label" for="address2">Giới tính</label>
                                    </div>
                                </div>
                                <div class="col-12 col-md-6 col-lg-4 mb-3">
                                    <div class="form-group form-floating">
                                        <select class="form-select" id="sltBank">
                                            <option>Chọn ngân hàng</option>
                                            @if (listBank != null && listBank.Count > 0)
                                            {
                                                foreach (var item in listBank)
                                                {
                                                    <option value="@item.Id" @(item.Id == accountData.Id ? "selected" : "")>@item.BankName</option>
                                                }
                                            }
                                        </select>
                                        <label class="form-control-label" for="address1">Ngân hàng</label>
                                    </div>
                                </div>
                                <div class="col-12 col-md-6 col-lg-4 mb-3">
                                    <div class="form-group form-floating">
                                        <input type="text" class="form-control" value="@accountData.BankAccount" id="inputBankAccount">
                                        <label class="form-control-label" for="address2">Chủ tài khoản</label>
                                    </div>
                                </div>
                                <div class="col-12 col-md-6 col-lg-4 mb-3">
                                    <div class="form-group form-floating">
                                        <input type="text" class="form-control" value="@accountData.BankNumber" id="inputBankNumber">
                                        <label class="form-control-label" for="address3">Số tài khoản</label>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row h-100 mb-4">
                <div class="col-12 d-grid"><a href="javascript:void(0)" id="btnUpdateInformation" class="btn btn-lg btn-default shadow-sm">Cập nhật thông tin</a></div>
            </div>

            <!-- change password -->
            <div class="row mb-3">
                <div class="col">
                    <h5 class="mb-0 text-white">ĐỔI MẬT KHẨU</h5>
                </div>
            </div>
            <div class="row">
                <div class="col-12">
                    <div class="card card-light shadow-sm mb-4">
                        <div class="card-body">
                            <div class="row h-100">
                                <div class="col-12 col-md-6 col-lg-4">
                                    <div class="form-floating  mb-3">
                                        <input type="password" class="form-control" placeholder="Mật khẩu mới"
                                               id="password">
                                        <label for="password">Mật khẩu mới</label>
                                    </div>
                                </div>
                                <div class="col-12 col-md-6 col-lg-4">
                                    <div class="form-floating ">
                                        <input type="password" class="form-control" placeholder="Xác nhận mật khẩu" id="confirmpassword">
                                        <label for="confirmpassword">Xác nhận mật khẩu</label>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row h-100 mb-4">
                <div class="col-12 d-grid"><a href="javascript:void(0)" id="btnChangePassword" class="btn btn-lg btn-default shadow-sm">Thay đổi mật khẩu</a></div>
            </div>

        </div>
        <!-- main page content ends -->

    *@




    <!DOCTYPE html>
    <html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Thông tin cá nhân</title>

        <!-- BOOTSTRAP -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
              integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">

        <!-- SWIPPER -->
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.css" />

        <!-- BASE CSS -->
        <link rel="stylesheet" href="~/Assets/publics/css/style.css">

    </head>

    <body>
        <div class="work-page info-user-page">
            <!-- NAV START -->
            <div class="navbar-page">
                <div class="icon col-2">
                    <a href="/">
                        <img src="~/Assets/publics/images/btn-out.png" width="100%" alt="">
                    </a>
                </div>
                <div class="text-nav col-8 logo">
                    <h5>Sửa đổi thông tin</h5>
                </div>
            </div>

            <div class="nav-bottom">
                <p>Copyright  c  2023 MCV Group, All Rights Reserved</p>
            </div>
            <!-- NAV END -->
            <!-- MAIN START -->
            <div class="main">

                <div class="info-user col-12">
                    <div class="col-3 avatar">
                        <img src="~/Assets/publics/images/avata.png" width="80%" alt="">
                    </div>
                    <div class="col-9 user-name">
                        <b>
                            <a href="">@memberSession.Username</a>
                        </b>
                        <p>
                            Số dư: <span class="moneyReload"></span>
                        </p>
                    </div>
                </div>
                <div class="form">
                    <form>
                        <div class="item-input">
                            <label for="name" class="col-4">Họ và tên:</label>
                            <input type="text" value="@accountData.FullName" class="col-8" id="inputFullName">
                        </div>
                        <div class="item-input">
                            <label for="name" class="col-4">Số điện thoại</label>
                            <input value="@accountData.PhoneNumber" inputmode="numeric" id="inputPhoneNumber" class="col-8">
                        </div>
                        <div class="col-12 mb-3">
                            <div class="item-input">
                                <label class="col-4" for="address2">Giới tính</label>
                                <select class="col-8" id="sltGender">
                                    <option value="Nam" @(accountData.Gender == "Nam" ? "selected" : "")>Nam</option>
                                    <option value="Nữ" @(accountData.Gender == "Nữ" ? "selected" : "")>Nữ</option>
                                </select>
                            </div>
                        </div>
                        <div class="col-12 mb-3">
                            <div class="item-input">
                                <label class="col-4" for="sltBank">Ngân hàng:</label>
                                <select class="col-8" id="sltBank">
                                    <option>Chọn ngân hàng</option>
                                    @if (listBank != null && listBank.Count > 0)
                                    {
                                        foreach (var item in listBank)
                                        {
                                            <option value="@item.Id" @(item.Id == accountData.Id ? "selected" : "")>@item.BankName</option>
                                        }
                                    }
                                </select>
                            </div>
                        </div>
                        <div class="item-input">
                            <label for="bank-accont" class="col-4">Số tài khoản:</label>
                            <input type="text" class="col-8" value="@accountData.BankNumber" id="inputBankNumber">
                        </div>
                        <div class="item-input">
                            <label for="bank-accont" class="col-4">Chủ tài khoản:</label>
                            <input type="text" class="col-8" value="@accountData.BankAccount" id="inputBankAccount">
                        </div>

                        <div class="item-button">
                            <button type="button" onclick="updateInformation()" id="btnUpdateInformation">Xác nhận</button>
                        </div>
                    </form>
                </div>

            </div>
            <!-- MAIN END -->


        </div>

        <!-- JQUERY -->
        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.0/jquery.min.js" integrity="sha512-3gJwYpMe3QewGELv8k/BX9vcqhryRdzRMxVfq6ngyWXwo03GFEzjsUm8Q7RZcHPHksttq7/GFoxjCVUjkjvPdw==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
        <!-- SWIPPER -->
        <script src="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.js"></script>
        <script src="~/Assets/toastr/toastr.js"></script>
        <script src="~/Assets/controller/base.js?v=@DateTime.Now.ToString("yyyyMMddHHmmss")"></script>
        <script src="~/Assets/controller/accounting.js"></script>
        
        <script>

            // SLIDER

            // NAV LEFT START
            $(document).ready(function () {

                $(".btn-nav").click(function () {
                    $(".left-bar").addClass("active");
                    $("body").addClass("active");
                });

                $(".background-left-bar").click(function () {
                    $(".left-bar").removeClass("active");
                    $("body").removeClass("active");
                });
            });
            // NAV LEFT END

            function updateInformation() {
                $.ajax({
                    url: '/cap-nhat-thong-tin',
                    type: 'post',
                    data: {
                        BankId: $('#sltBank').val(),
                        BankAccount: $('#inputBankAccount').val(),
                        BankNumber: $('#inputBankNumber').val(),
                        PhoneNumber: $('#inputPhoneNumber').val(),
                        FullName: $('#inputFullName').val(),
                        Gender: $('#sltGender').val()
                    },
                    beforeSend: function () {
                        $('#btnUpdateInformation').prop('disabled', true);
                    },
                    success: function (res) {
                        $('#btnUpdateInformation').prop('disabled', false);
                        if (res.status) {
                            base.success(res.msg);
                        }
                        else {
                            base.error(res.msg);
                        }
                    }
                });
            }

        </script>

    </body>

</html>
}